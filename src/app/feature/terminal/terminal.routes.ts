import { Routes } from '@angular/router';
import { TerminalListPage } from '../terminal/pages/terminal-list-page';

export const routes: Routes = [
  {
    path: '',
    component: TerminalListPage
  }
];
